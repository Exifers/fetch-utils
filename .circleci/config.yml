version: 2
jobs:
  test:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run: npm install
      - run: npm run test

workflows:
  version: 2
  basic:
    jobs:
      - test
